# MK GADGETS

Ecommerce-style marketing site for MK GADGETS (Ghana): product catalog, shopping cart, and checkout. Built with [Next.js](https://nextjs.org) (App Router) and Tailwind CSS.

## Getting started

Install dependencies and run the dev server:

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Scripts

- `npm run dev` — development server
- `npm run build` — production build
- `npm run start` — serve production build
- `npm run lint` — ESLint

## Environment

Copy `.env.example` to `.env.local` and set values (phone, WhatsApp, site URL, optional Resend for inquiry/order emails). Do not commit secrets.

## Deploy

Deploy on [Vercel](https://vercel.com) or any Node host; set `NEXT_PUBLIC_SITE_URL` to your production URL.
